module.exports = {
    SERVER_PORT: 8080,       // this port number must be available on the server
    AUDIO_LIBRARY_PATH: "",  // absolute path from the server root
    IMAGE_LIBRARY_PATH: "",  // absolute path from the server root
    DB_HOST: "localhost",    // domain name or IP address of database server (probably localhost/ 127.0.0.1 )
    DB_NAME: "",             // name of the database [DB_NAME]
    DB_USER: "",             // database user [DB_USERNAME]
    DB_PASS: ""              // database user's password
};
